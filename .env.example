# Copy this to .env and fill in real values. Never commit .env itself.

# Apify token — use a SEPARATE Apify account from the one rr-tracker uses,
# since that account is at capacity.
APIFY_TOKEN=

# Only needed if running the generation scripts standalone, outside of a
# live Claude Code session.
ANTHROPIC_API_KEY=
